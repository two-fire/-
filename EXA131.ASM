;FILENAME: EXA131.ASM
;对程序进行修改
.486
DATA SEGMENT USE16
SUM  DB ?,? ;?,?,
MESG DB '25+9='
     DB 0,0,'$';0,0
N1   DB 9,0FH  ;F0H
N2   DW 25   
DATA ENDS

CODE SEGMENT USE16
    ASSUME CS:CODE,DS:DATA
BEG:
    MOV AX,DATA
    MOV DS,AX
    MOV BX,OFFSET SUM
    MOV AH,N1
    MOV AL,BYTE PTR N2;AL,N2
    ADD AH,AL
    MOV [BX],AH
    CALL CHANG
    MOV AH,9
    MOV DX,OFFSET MESG  ;OFFSEG MEST
    INT 21H
    MOV AH,4CH
    INT 21H
CHANG PROC;CHANG: PROC
LAST: CMP BYTE PTR[BX],10;[BX],10
	  JC NEXT
	  SUB BYTE PTR[BX],10;[BX],10
	  INC BYTE PTR[BX+7];[BX+7]
	  JMP LAST
NEXT: MOV AL,[BX];添加一句
	  ADD BYTE PTR[BX+8],AL;[BX+8],SUM
	  ADD BYTE PTR[BX+7],30H;[BX+7],30H
	  ADD BYTE PTR[BX+8],30H;[BX+8],30H
	  RET
CHANG ENDP;CHANG: ENDP
CODE ENDS
    END BEG



